# LLM Router Functional Test Suite Configuration
# Copy this file to .env and update with your actual values

# =============================================================================
# 🔑 API CONFIGURATION (REQUIRED)
# =============================================================================

# Your LLM Router API endpoint
# Example: http://your-server:3000 or https://api.llmrouter.dev
BASE_URL=http://your-server:3000

# Persistent API key for testing (recommended)
# See SETUP-PERSISTENT-KEY.md for setup instructions
# This key survives server restarts and project updates
ROUTER_API_KEY=llm_test_persistent_key_fixed_2025.persistent_test_secret_never_changes_mikecerqua_2025_llm_router

# Legacy API key format (if not using persistent key)
# API_KEY=your-api-key-here

# =============================================================================
# 🧪 FUNCTIONAL TEST CONFIGURATION
# =============================================================================

# Request timeout in seconds for AI operations (default: 60)
# AI inference can take longer than simple HTTP requests
REQUEST_TIMEOUT=60

# Maximum tokens for AI generation tests (default: 150)
MAX_TOKENS=150

# Temperature settings for creativity tests
TEMPERATURE_LOW=0.2
TEMPERATURE_HIGH=0.9

# Model configuration
TEST_MODEL=simple-fallback
AUTO_DETECT_FORMAT=true

# =============================================================================
# 📊 OUTPUT CONFIGURATION
# =============================================================================

# Enable detailed AI response logging (true/false)
VERBOSE_LOGGING=true

# Save detailed test logs (true/false)
SAVE_LOGS=true

# Log file location
LOG_FILE=/tmp/llm-router-functional-tests.log

# Results directory for JSON output
RESULTS_DIR=./test-results

# Console output colors (true/false)
ENABLE_COLORS=true

# =============================================================================
# ⚡ PERFORMANCE TEST SETTINGS
# =============================================================================

# Number of concurrent requests for performance testing (default: 3)
CONCURRENT_REQUESTS=3

# Performance test timeout (default: 30 seconds)
PERFORMANCE_TIMEOUT=30

# Response time thresholds (seconds)
FAST_RESPONSE_THRESHOLD=5
ACCEPTABLE_RESPONSE_THRESHOLD=10

# =============================================================================
# 🛡️ SECURITY & AUTHENTICATION
# =============================================================================

# User agent string for requests
USER_AGENT=LLM-Router-Functional-Tests/1.0

# Skip SSL verification (ONLY for testing - default: false)
# WARNING: Only set to true for local development
SKIP_SSL_VERIFY=false

# Authentication method (bearer/header)
AUTH_METHOD=bearer

# =============================================================================
# 🧭 ROUTING STRATEGY CONFIGURATION
# =============================================================================

# Test routing strategies
TEST_QUALITY_ROUTING=true
TEST_COST_ROUTING=true
TEST_BALANCED_ROUTING=true

# Strategy-specific parameters
QUALITY_STRATEGY_TIMEOUT=30
COST_STRATEGY_TIMEOUT=20
BALANCED_STRATEGY_TIMEOUT=25

# =============================================================================
# 💬 CHAT & INFERENCE SETTINGS
# =============================================================================

# Chat test configuration
CHAT_MAX_TOKENS=100
CHAT_TEMPERATURE=0.7
CHAT_TIMEOUT=60

# System prompt test settings
SYSTEM_PROMPT_ENABLED=true
SYSTEM_PROMPT_TIMEOUT=45

# Multi-turn conversation settings
CONVERSATION_CONTEXT_ENABLED=true
MAX_CONVERSATION_TURNS=3

# =============================================================================
# 🔧 ADVANCED DEBUGGING
# =============================================================================

# Debug mode (shows curl commands and raw responses)
DEBUG_MODE=false

# Fail fast on first error (true/false)
FAIL_FAST=false

# Retry failed tests (number of retries, default: 0)
RETRY_COUNT=0

# Test isolation (clean state between tests)
ISOLATE_TESTS=true

# =============================================================================
# 📋 TEST SELECTION
# =============================================================================

# Enable/disable test categories (true/false)
RUN_MODEL_TESTS=true
RUN_ROUTING_TESTS=true
RUN_CHAT_TESTS=true
RUN_BEHAVIOR_TESTS=true
RUN_ERROR_TESTS=true
RUN_PERFORMANCE_TESTS=true
RUN_SECURITY_TESTS=true

# Skip specific tests (comma-separated test numbers)
# Example: SKIP_TESTS=5,7,12
SKIP_TESTS=

# Run only specific tests (comma-separated test numbers)
# Example: ONLY_TESTS=1,2,3,4
ONLY_TESTS=

# =============================================================================
# 🚨 IMPORTANT NOTES
# =============================================================================

# 1. For persistent API key setup, see SETUP-PERSISTENT-KEY.md
# 2. Ensure your LLM Router server is running and accessible
# 3. Test with quick-test.sh before running full functional tests
# 4. Check /tmp/llm-router-functional-tests.log for detailed logs
# 5. All tests validate REAL AI capabilities, not mocks!

# =============================================================================
# 📖 USAGE EXAMPLES
# =============================================================================

# Basic setup for local testing:
# BASE_URL=http://localhost:3000
# ROUTER_API_KEY=your-persistent-key

# Production testing:
# BASE_URL=https://api.yourdomain.com
# ROUTER_API_KEY=your-production-key
# REQUEST_TIMEOUT=120

# Debug mode for troubleshooting:
# DEBUG_MODE=true
# VERBOSE_LOGGING=true
# FAIL_FAST=true